node_modules/
dist/
.next/
.turbo/
coverage/
*.tsbuildinfo

# secrets — never commit a populated env file
.env
.env.*
!.env.example

# local postgres cluster created by scripts/dev-db.sh
.pgdata/

*.log
.DS_Store

# Copied from node_modules at dev/build time; see apps/web/scripts/copy-map-worker.mjs
apps/web/public/maplibre-gl-worker.mjs
apps/web/public/maplibre-gl-shared.mjs

# python services
**/.venv/
**/__pycache__/
**/.pytest_cache/

# v2 env template is a template, not a secret

# per-run harness output; the baseline is the committed record
services/resolution/eval/latest.json
