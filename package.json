{
  "name": "scout",
  "private": true,
  "version": "0.1.0",
  "description": "Tiered OSINT investigation platform — a launcher, not an aggregator.",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=20.11"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "db:generate": "pnpm --filter @scout/db exec prisma generate",
    "db:migrate": "pnpm --filter @scout/db exec prisma migrate dev",
    "db:deploy": "pnpm --filter @scout/db exec prisma migrate deploy",
    "db:seed": "pnpm --filter @scout/db run seed",
    "db:studio": "pnpm --filter @scout/db exec prisma studio"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@prisma/client",
      "@prisma/engines",
      "esbuild",
      "prisma"
    ]
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "turbo": "^2.3.3",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
